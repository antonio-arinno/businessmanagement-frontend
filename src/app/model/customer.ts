import { Address} from './address';

export class Customer {

  id: number;
  code: string;
  taxId: string;
  name: string;
  createAt: string;
  address: Address;
  telephone: string;
  email: string;
}
